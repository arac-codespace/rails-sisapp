Added new personal care
<% if activity.trackable %>
    <!-- The second activity.trackable is the path for the link_to! -->
    <%= link_to activity.trackable.title, activity.trackable %> article! Date: <%= activity.trackable.created_at.to_time.strftime('%a %b %d %H:%M:%S %Z %Y') %>. 
<% else %>
    article has since been removed!
<% end %>

